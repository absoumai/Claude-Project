// Copy to secrets.ts and fill in. secrets.ts is gitignored — never commit it.
// Supabase env vars of the same name override these if you set them in the dashboard.
export const API_KEY = "sk-ant-...";
export const PASSCODE = "choose-a-passcode";
